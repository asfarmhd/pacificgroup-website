# OS and editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
.cursor/

# Optional: uncomment if you keep env or secrets locally (never commit real FTP password)
# .env
# *.local

# Dependencies / build (none for this static site; keep for future)
node_modules/

# Plan/spec files (optional; remove if you want them in repo)
# .cursor/
# *.plan.md

# Local source-asset handoff (227 MB) - not part of the deployed site
wordpress website content/
